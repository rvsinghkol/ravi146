export isrunning=`ps -ef | grep NgSettlementUtility.jar | grep -v grep | wc -l `

echo $isrunning
if [ $isrunning -eq 0 ] 

then
	echo "NOT RUNNING" ;
	
cd /opt/SettlementRelease/
	
  #nohup /opt/SettlementRelease/run.sh 1>/dev/null &

  else

echo "RUNNING" ;

fi
