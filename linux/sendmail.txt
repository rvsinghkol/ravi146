# vi /etc/sysconfig/networks
Note:- update 'support.ctrls.in' as hostname

# hostname support.ctrls.in

# vi /etc/hosts
IP support.ctrls.in loylty

# /etc/init.d/postfix status

# /etc/init.d/postfix stop

# rpm -qa sendmail*

# yum install sendmail*

# rpm -qa sendmail*

# rpm -qa m4*

# vi /etc/mail/sendmail.mc

  Note:-Modify 116 line: 127.0.0.0 to 0.0.0.0

# /etc/init.d/sendmail restart

# chkconfig sendmail on 

# hostname

# netstat -ntlp | grep sendmail

# telnet smtp.gmail.com 25

# telnet client-ip 25

# nslookup google.com
# nslookup gmail.com
# dig mx gmail.com
# dig gmail-smtp-in.l.google.com

# mail -v prashatkumar.chawan@gmail.com

# tail -f /var/log/maillog

# cd /var/spool/mail
# echo /dev/null > nagios
# echo /dev/null > root
# cat /etc/aliases
# cat nagios root

# cd /usr/local/nagios/etc/objects
# grep contact *

# cd ../LOYLTY
# grep contact *

# /usr/local/nagios/bin/nagios -v /usr/local/nagios/etc/nagios.cfg

# mailq

# cd /vat/spool/mailqueue
# rm -rf *




