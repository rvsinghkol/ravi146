MYSQL:
http://www.yolinux.com/TUTORIALS/LinuxTutorialMySQL.html
CREATE USER 'testuser'@'localhost' IDENTIFIED BY 'test123test!';
GRANT CREATE ON *.* TO 'testuser'@'localhost';(Grant all database permission)
GRANT DROP ON tutorial_database.* TO 'testuser'@'localhost';( ability to drop tables in the specific database)
SHOW GRANTS FOR 'testuser'@'localhost';
REVOKE permission ON database.table FROM 'user'@'localhost';
DROP USER 'testuser'@'localhost';(Remove a MySQL User)


Nagios Host COnfiguratio..............................

This file (/usr/local/nagios/etc/objects/xxxxxxxx.cfg) has a host that is referencing the windows-server hostgroup, 
that hostgroup needs to be defined somewhere (like /usr/local/nagios/etc/objects/hostgroups.cfg).
Make sure that it's included in your nagios.cfg
cfg_file=/usr/local/nagios/etc/objects/hostgroups.cfg

snmp configuration
vmwarebtolls installation
firewall disable
change the ssh port 2244


snmp configuration
yum -y install net-snmp net-snmp-utils
vi /etc/snmp/snmpd.conf
rocommunity  public(ricohidc)
service snmpd restart.
apt-get purge --auto-remove smp snmpd

vmwarebtolls installation
ls
Centos :
1.VM - GUEST - CLICK VMWARE INSTALL
2.mount /dev/cdrom /mnt/
3.cd /mnt/
4.ls
5.cp vmware tools - 9.0.0 - 782409.tar.gz /root/
6.cd /root/
7.tar xzvf VMwareTools-9.2.2-893683.tar.gz
8.ls
9. ./vmware - install.pl
10.finish

Ubuntu :

1. sudo to under root 
syndinet@
Next :

Same Previous Process

firewall disable
service iptables stop
chkconfig iptables off
Then service iptables restart
service network restart

###change the ssh port 2244
vim /etc/ssh/sshd-config
change port no
service sshd restart

yum create in centos.
 Create directory & copy all centos file.
#vi /etc/yum.respons.d/yum.repo
 basename]
baseurl=file:///test/Packages
enable=1
gpgcheck=0
:wq
#cd /test/Packges
# rpm -ivh createrep*
#createrep -v /test/Packegs


1).what is kernel sync error
Kernel panic - not syncing: No init found
2)

2)SELinux not using
# /etc/rc.d/init.d/iptables stop 
# chkconfig iptables off 
# chkconfig ip6tables off 
# vi /etc/selinux/config 
SELINUX=disabled    # change
# reboot

How to configure snmp on Debian

1. nano /etc/apt/sources.list

2. deb http://ftp.fr.debian.org/debian/ wheezy main contrib non-free

after adding the line run the next command

3. apt-get update
4. apt-get install snmpd snmp-mibs-downloader
5. nano /etc/default/snmpd  export MIBS=ALL
6. vi /etc/snmp/snmpd.conf

delete all the lines :1,$d

Then add rocommunity ricohidc

Then save the file.

Service snmpd start


##Google crome installation
dpkg -i google-chrome-stable_current_i386.deb 

Camtesia key..
RegistrationKey=KC2AC-ADZAM-CSWAW-NF6XZ-N56F8

https://dl.google.com/linux/direct/google-chrome-stable_current_i386.rpm